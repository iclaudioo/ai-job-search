{
  "name": "vdab-cli",
  "version": "1.0.0",
  "description": "CLI for searching jobs on VDAB Vind een job, the Flemish public employment service's job bank. Search runs over a plain fetch with zero runtime dependencies; detail pages are rendered through the firecrawl CLI because VDAB renders them client-side. Personal use only.",
  "type": "module",
  "main": "src/cli.ts",
  "bin": {
    "vdab-search": "src/cli.ts"
  },
  "scripts": {
    "start": "bun run src/cli.ts",
    "test": "bun test --timeout 120000",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/bun": "1.3.14"
  }
}
