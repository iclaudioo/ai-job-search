{
  "name": "stepstone-cli",
  "version": "1.0.0",
  "description": "CLI for searching jobs on StepStone Belgium. Search runs over a plain fetch with zero runtime dependencies; detail pages are rendered through the firecrawl CLI because StepStone blocks direct requests to them. Personal use only.",
  "type": "module",
  "main": "src/cli.ts",
  "bin": {
    "stepstone-search": "src/cli.ts"
  },
  "scripts": {
    "start": "bun run src/cli.ts",
    "test": "bun test --timeout 120000",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/bun": "1.3.14"
  }
}
